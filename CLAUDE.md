# CLAUDE.md — Project VACANCY 憲法(v0.2)

> 太陽近傍・空き度アトラス。起草 2026-08-23。
> 本文書は本プロジェクトの最上位規範である。PHASES.md・裁定ログ・全コードはこれに従属する。
> 改訂は裁定ログに理由を残して行う(WAKE/TESSERA/ASTROGATION/EMBARK の運用を継承)。

---

## 第1条 目的と立場

1. 本プロジェクトは、太陽近傍の恒星系ごとに「宣言された技術帯の設備で占有されているなら既存サーベイで痕跡が検出されていたはずである」という条件付き不在証拠を合算し、**星単位・技術帯別の占有確率上界(空き度)** を初めて計算する。
2. 空き度は **測量であって証明ではない**。不在の証拠は限定的であり、占有の不在を証明しない。この一文は論文・台帳・シミュレータの全てに常時表示する。
3. 動機は二つ(依頼者の言葉): (i) 侵略ではなく「空いているところ」を探すことで人類の生息域拡張に寄与する、(ii) 宇宙の謎を解く。本憲法において倫理条件(侵略しない)は方法論(条件付き確率文)に昇格しており、両者は分離できない。
4. 空き度は **入植許可証ではない**。本成果物から占有の不在・入植の正当性を推論することを、論文の解釈規律の節で明示的に禁ずる。

## 第2条 体制

- 人間(前田幸枝): 方向設定、ゲート裁定、全ての分岐点の最終決定。
- チャット Claude: 参謀、独立検算(G2 第二経路)、文献照合。
- Claude Code (Fable 5): 実行。effort high 基調、難所のみ xhigh。
- 出力状態と失敗状態を指定し、実装方法は委任する。対症療法は禁止、構造的根本治療のみ。物理先行裁定(制約を先に解き、残った自由度で美を最大化)。

## 第3条 空き度の定義(解毒規約 — 本憲法の核心)

1. 技術帯 T を **宣言されたクラスの有限集合** として定義する。v1 の暫定帯(Phase 0 で確定):
   - T-R1: 等方狭帯域送信、EIRP ≥ 10^13 W 級(Arecibo 惑星レーダー相当)
   - T-R2: 等方狭帯域送信、EIRP ≥ 10^17 W 級(恒星級電力の一部を送信に充てる)
   - T-R3: 地球級漏洩(EIRP ~10^9–10^11 W、非定常)— 自己検定帯
   - T-W1: 廃熱(中赤外超過、Ĝ γ 閾値で定義)
   - 追加帯(光学レーザー、遷移現象等)は拡張区画とし、v1 では判定不能扱い。
2. サーベイ s・星 i・技術帯 T について、**検出確率 ε_{s,i}(T)** を「星 i に T 帯の設備が存在するならばサーベイ s がそれを検出していた確率」として定義する。EIRP 閾値(距離依存)、周波数帯被覆、稼働率、ビーミング立体角、観測時刻・積分時間の全ての不一致は **ε の内部でのみ** 処理する。合算式はこれらを一切参照しない。
3. 合算は不検出データ D のもとでの尤度積とする:
   P(D | i 占有, T) = Π_s (1 − ε_{s,i}(T))
   事後占有確率 P(占有 | D, T, π) = π Π(1−ε) / (π Π(1−ε) + 1 − π)
4. **空き度は事前確率 π の関数(上界曲線)として出す。** 単一の数値を「空き度」と呼んではならない。台帳には ε の積 Λ_i(T) = Π_s (1−ε_{s,i}(T)) と、代表 π での事後値を併記する。
5. ε の独立性仮定(サーベイ間、稼働率の相関)は台帳の来歴欄に宣言する。同一望遠鏡・同一帯域の重複観測は独立とみなさず、最大被覆で併合する。
6. **絶対的な「空いている」は主張しない。** 全ての文は「T 帯の設備に関して、サーベイ集合 S のもとで」という条件節を伴う(WAKE 第7条の直系)。

## 第4条 三軸の役割分担

1. 空き度(第3条)— 新規・主戦場。世界に存在しない量。
2. 到達可能性 — EMBARK reachability atlas をそのまま輸入し、独自計算は行わない。
3. 定住資源性 — GCNS・既知系外惑星・スペクトル型等の既製カタログの輸入に限る。狭義ハビタビリティ(液体の水・温度)は判定基準にせず、定義の関数(狭義〜資源的広義のスライダー)として軸に置く。HWC 等は G3 照合台としてのみ用いる。
4. 三軸を単一スコアに圧縮してはならない。交差表示(「行ける × 空いている」)は許すが、積・加重和は禁止。

## 第5条 判定不能会計

1. 以下は「判定不能」と記し、空き度 0 とも 1 とも扱わない:
   (a) いずれのサーベイの視野にも入っていない星、(b) 上限値が本文・図にのみ存在し機械可読化未了の星、(c) 拡張区画の技術帯、(d) ε の前提が文献から復元できないサーベイ。
2. 判定不能は台帳・アトラス・論文の全てで明示的に計数し、母集団に対する比率を報告する(WAKE 第5条6項の継承)。
3. 判定不能を「空き」側に倒すことを禁ずる。

## 第6条 データ階層

- 第1層(v1 成立要件): 機械可読の既存上限のみ — BL 公開アーカイブ、Wlodarczyk-Sroka+20 表、Ĝ/WISE 廃熱台帳。
- 第2層(拡張区画): 論文本文・図からの抽出。来歴欄に「図から読取」を明記し、数値の不確かさを別枠で持つ。
- 恒星基盤は Gaia DR3(GCNS)。DR4(2026-12-02)合流時に v1.1 へ差替える二段構え。
- 全台帳行にサーベイ来歴(文献 ID、観測日、帯域、閾値、ε 導出式のバージョン)を付す。

## 第7条 検証ゲート

- **G1 単調性と安定性**: サーベイを追加しても Λ_i(T) が増加しないこと(単調非増加)を全星・全帯で機械検査。ε のパラメータ摂動(MC)に対する事後上界の安定性を報告。
- **G2 二経路合成**: 独立実装の尤度積(Claude Code)と、チャット Claude による解析近似(対数和)が全星で一致すること。不一致は裁定ログへ。
- **G3 アンカー**:
  (i) Wlodarczyk-Sroka+20 の母集団上限を、**同一の ε 前提のもとで計数版(頻度論)として再現**する。星単位ベイズ上界の周辺化は別量であり、差を明示する(一致を強要しない)。
  (ii) Ĝ の個別星上限を T-W1 帯で再現。
  (iii) 自己検定: 太陽系を T-R3 帯で台帳に通す。期待される結果は「不検出 → 事後 ≈ 事前」であり、これは不合格ではなく **空き度が語れる技術帯の下限を地球自身で校正する** 結果として報告する。
- G1–G3 のいずれかが未達のまま Phase 5(公開)に進むことを禁ずる。

## 第8条 差別化の明示

論文は必ず次を位置づけ節に書く: Grimaldi 系(確率論的母集団モデル)に対して本作は星単位台帳であること、Wright+18 ヘイスタックに対して本作は全空間体積ではなく個別星への射影であること、BL・Ĝ に対して本作は新規観測ではなく既存不在証拠の異種合算であること。

## 第9条 公開文化

1. 三点固定(論文・台帳・シミュレータ)、判定不能明示、誤り台帳を継承する。
2. **二段の事前登録**:
   (a) Phase 0 末: 技術帯集合(第3条1項)と ε の因子構造(どの因子を含めるか)を固定し、コミットハッシュを裁定ログに記録する(非公開)。
   (b) Phase 1 末: ε の具体閾値・合算式・G1–G3 の合格基準を固定し、Zenodo に事前登録してから合算を走らせる(公開)。
3. (a)(b) 以後の定義変更は全て「逸脱」として裁定ログと論文に明記する。結果を見てからの ε 変更は逸脱扱いを免れない。
4. ホールドアウト(母集団の二分)は判定不能率の確定後に採否を裁定する。
5. 経路は Zenodo DOI → arXiv → MNRAS/AJ 圏。

## 第10条 禁止事項

1. 空き度を単一数値・単一順位として提示すること。
2. 技術帯の条件節を省いた「空いている」という文。
3. 判定不能を空き側に倒すこと。
4. 三軸の積・加重和。
5. ε の不一致を合算側の係数で誤魔化すこと(対症療法の典型)。
6. 本成果物を入植・接触・送信の正当化に用いること、またはそう読める記述。
