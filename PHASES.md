# PHASES.md — Project VACANCY(v0.2)

> 各 Phase は「出力状態」と「失敗状態」で定義する。方法は Claude Code に委任。
> 分岐点(★)は人間の裁定を待つ。ゲート G1–G3 は CLAUDE.md 第7条。

---

## Phase 0 — 実査と数理設計(前提潰し済みの初期条件から)

**出力状態**
- 0.1 機械可読性実査: BL 公開アーカイブ・Wlodarczyk-Sroka+20 表・Ĝ/WISE の各々について、(a) 取得手段、(b) 星 ID と Gaia DR3 source_id の照合率、(c) ε 導出に必要な欄(閾値・帯域・積分時間・観測日)の充足率、を表にする。
- 0.2 技術帯集合の確定案: T-R1/R2/R3/W1 の閾値を数値で固定し、各帯で ε_{s,i}(T) の導出式を 1 本ずつ書く(距離・感度・帯域被覆・稼働率・ビーミングの各因子を明示)。
- 0.3 合算数理の仕様: 尤度積、対数和による数値安定化、π 掃引の範囲、MC 摂動の設計。
- 0.4 計算予算実測: 第1層の星数 × 帯数 × サーベイ数で所要時間を実測(想定: CPU 軽量、分単位)。
- 0.5 EMBARK 到達可能性・GCNS 定住資源性の輸入フォーマット確認。

**失敗状態**
- 第1層の星・source_id 照合率が 80% 未満 → ★ 第2層を v1 に繰り上げるか、母集団を絞るか裁定。
- いずれかの帯で ε の導出式が文献から閉じない → その帯を拡張区画へ降格(判定不能)。

- 0.6 **事前登録(a)**: 技術帯集合と ε の因子構造を固定し、コミットハッシュを裁定ログに記録(第9条2項a)。

**★ 分岐**: 技術帯集合の確定(第3条1項を書き換える唯一の機会)。

---

## Phase 1 — ε 台帳の構築と事前登録

**出力状態**
- 1.1 星 × サーベイ × 帯の ε 台帳(JSON)。各セルに来歴と導出式バージョン。
- 1.2 独立性仮定の宣言文(第3条5項)。
- 1.3 判定不能の初回計数(第5条)。
- 1.4 **事前登録(b)**: ε の具体閾値・合算式・G1–G3 の合格基準を Zenodo に固定(合算結果を見る前に、第9条2項b)。
- 1.5 判定不能率を踏まえ、ホールドアウトの採否を ★ 裁定。

**失敗状態**
- 判定不能が第1層母集団の 50% を超える → ★ 母集団定義(距離上限)を見直すか、そのまま報告するか裁定。

---

## Phase 2 — 合算と G1/G2

**出力状態**
- 2.1 Λ_i(T) と π 掃引の事後上界曲線を全星・全帯で計算。
- 2.2 G1: 単調性の機械検査(全セル合格)、MC 摂動の安定性レポート。
- 2.3 G2: チャット Claude の解析近似との突合。全星一致(許容差は Phase 0 で固定)。

**失敗状態**
- G1 単調性違反 → ε 台帳のバグ(合算側を触らない)。根本原因を裁定ログへ。
- G2 不一致 → 両経路を再導出。係数で合わせることを禁ずる。

---

## Phase 3 — G3 アンカー

**出力状態**
- 3.1 Wlodarczyk-Sroka+20 の計数版再現(同一 ε 前提)。ベイズ周辺化との差を表に。
- 3.2 Ĝ 個別星上限の T-W1 再現。
- 3.3 自己検定: 太陽系 T-R3。「事後 ≈ 事前」の確認と、空き度が語れる技術帯下限の校正値。

**失敗状態**
- 3.1 の計数版が公刊値と桁で合わない → ε の距離依存か母集団定義の誤り。ε 側を直す。
- 3.3 で地球が「検出される」と出る → ε の過大評価(最重要の赤信号)。

**★ 分岐**: G3 合格判定。

---

## Phase 4 — 三軸アトラス

**出力状態**
- 4.1 到達可能性(EMBARK)・定住資源性(GCNS 等)を同一 source_id で結合。
- 4.2 定住資源性スライダー(狭義〜広義)の定義と、HWC との照合表。
- 4.3 交差表示「行ける × 空いている(T 帯条件付き)」。積・加重和なし(第4条4項)。

**失敗状態**
- 軸の結合で source_id 不一致が出る → 結合率を報告し、不一致星は判定不能。

---

## Phase 5 — 公開三点セット

**出力状態**
- 5.1 論文(日英)。構成: 位置づけ(第8条)、ε 定義、合算、G1–G3、三軸、解釈規律(第1条2・4項)、判定不能会計、誤り台帳。
- 5.2 台帳 JSON(星単位・帯別・来歴付き)+ アトラス。
- 5.3 シミュレータ: wake-atlas 第三面(空き度レイヤ)として実装。π スライダーと技術帯切替を UI に持ち、条件節を常時表示。
- 5.4 Zenodo DOI → arXiv → MNRAS/AJ 圏。

**失敗状態**
- G1–G3 いずれか未達 → 公開禁止(第7条)。

---

## Phase 6 — DR4 合流(2026-12-02 以降)

- 恒星基盤を DR4 に差替え、到達可能性軸(EMBARK v1.1)と同期して v1.1 を出す。ε 台帳は距離更新分のみ再計算。
